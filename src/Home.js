import React from "react";
import { Button, ButtonGroup } from '@chakra-ui/react'
import {
  Alert,
  AlertIcon,
  AlertTitle,
  AlertDescription,
} from '@chakra-ui/react'

const Home = () => {
  return (
    <div>
      <Button colorScheme='blue'> KLIK </Button>
    </div>
  )
}

export default Home;